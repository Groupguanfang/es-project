import { defineConfig } from 'tsup'

export default defineConfig({
  entry: {
    {{#each (split entry) as |file|}}
    {{file}}: './src/{{file}}.ts',
    {{/each}}
  },
  dts: {{dts}},
  sourcemap: {{sourcemap}},
  clean: {{clean}},
  format: [{{#each format}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}}],
});
