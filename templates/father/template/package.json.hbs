{
  "name": "{{name}}",
  "type": "module",
  "version": "1.0.0",
  "description": "{{description}}",
  "keywords": [
    "es-project-template"
  ],
  "es-project": {
    "version": 1,
    "generatorScript": "./dist/plopfile.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "scripts": {
    "build": "tsup",
    "test": "tsx test/index.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "es-project": "{{#if isInEsProject}}workspace:*{{else}}latest{{/if}}"
  }
}
